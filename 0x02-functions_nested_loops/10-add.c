#include <stdio.h>
#include "main.h"
/**
 * add - sums 2 numbers
 * @i : is first digit
 * @j : is second digit to add to a
 * Return: on success c
 */
int add(int i, int j)
{
	int c = i + j;

	return (c);
}
