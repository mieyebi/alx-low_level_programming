#include <stdio.h>
#include "main.h"

/**
* _abs - computes the absolute value of an integer
*
* Return: 0 on success
*/
int _abs(int)
{
	int n, a;

	n = 0;

	if (a >= n)
	{
		_putchar(a);
	}
	else
	{
		_putchar(-a);
	}
	return (0);
}
