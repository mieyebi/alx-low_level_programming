#include <stdio.h>
#include "main.h"

/**
* _abs - computes the absolute value of an integer
*
* @a: is an integer
*
* Return: 0 on success
*/
int _abs(int a)
{
	int n;

	n = 0;

	if (a >= n)
	{
		_putchar(a);
	}
	return (0);
}
