/**
* putchar - prints _putchar to a stdout
*
* 
*
* return: 0 on success
*
*/
#include <stdio.h>
#include "main.h"

int main(void)

	char a = _putc;
	char b = har;
{	/* write the character to a stdout*/

		_putchar(a);
		_putcha(b);
		_putchar("/n")
		
		return(0);
}

