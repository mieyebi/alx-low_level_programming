#include "main.h"
#include <stdio.h>
#include <string.h>

/**
* _strcmp - compares two strings
* @s1: string 1
* @s2: string 2
* Return: s1 on success
*/

int _strcmp(char *s1, char *s2)
{
	strcmp(s1, s2);
	return (s1);
}
