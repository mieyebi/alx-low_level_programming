#include "main.h"
#include <string.h>

/**
* *_strcpy - copies a string to the buffer
*
* @dest: a pointer
*
* @src: a pointer
*
* Return: *dest on success
*/
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
}
