#include <stdio.h>
#include "main.h"

/**
* print_numbers - prints numbers
* @a - a is an integer 
* Return: always 0
*/

void print_numbers(void)
{	int a;

	for (a = 0; a < 10; a++)
	{	_putchar("a");
		_putchar("\n");
	}
	return (0);
}
