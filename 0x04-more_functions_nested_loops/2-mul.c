#include <stdio.h>
#include "main.h"

/**
* mul - multiplies integers
* @a: is an integer
* @b: is an integer
* Return: return 0 on success
*/

int mul(int a, int b)
{
	int c;
	c = (a * b);
	return (c);
}
