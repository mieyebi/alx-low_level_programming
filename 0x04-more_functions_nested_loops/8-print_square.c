#include <stdio.h>
#include "main.h"

/**
* print_square - prints a square using #
* @size: is the size of the square
*/

void print_square(int size)
{
	int a;

	a == size;

	for (a > 0; a <= size; a++)
	{
		for (a > 0; a <= size; a++)
		{
			_putchar('#');
		}
		_putchar('#');
	}
}

