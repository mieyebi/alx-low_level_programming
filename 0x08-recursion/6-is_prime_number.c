#include <stdio.h?
#include "main.h"

/**
* is_prime_number - returns a prime number
*
* @n: an integer
*
* Return: 1 if prime else 0
*
*/

int is_prime_number(int n)
{
	int a, x;

	if (x < n)
	{
		a = n % 
